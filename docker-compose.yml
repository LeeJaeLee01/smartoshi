services:
  backend:
    build: .
    ports:
      - "3030:3030"
    environment:
      PORT: "3030"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "8080:3000"
    depends_on:
      - backend
    restart: unless-stopped
